/target
**/*.rs.bk
.env
# Local config carries real app secrets — commit only cola.toml.example
cola.toml
# Live E2E test credentials — commit only cola-test.toml.example
cola-test.toml
# Test artifacts produced by the AI agent during live debugging
SURVEY.md
# Live run logs (contain transient command echoes)
test.log
